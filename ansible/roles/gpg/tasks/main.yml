---

-   name: Install GPG and dependencies
    yum:
        name: "{{ item }}"
        state: present
    with_items:
        - gnupg2
        - rng-tools
        - pass

-   include: usershelld.yml
