---

# Inputs
#   {{username}}

-   name: Create gen key batch script
    template:
        src: templates/home/user/.gnupg/gpg-agent.conf
        dest: "/home/{{username}}/.gnupg/gpg-agent.conf"
        owner: "{{username}}"
        group: "{{username}}"
        mode: 0600
