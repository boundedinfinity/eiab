-   name: Set clock on control node
    hosts: localhost
    connection: local
    become: yes

    tasks:
    
        -   include_role:
                name: ntp

-   name: Set clock on all node
    hosts: all
    become: yes

    tasks:
    
        -   include_role:
                name: ntp
