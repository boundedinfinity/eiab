-   name: Generate ssh keypair
    hosts: localhost
    connection: local
    become: yes

    tasks:
        -   include_role:
                name: eiab
                tasks_from: control-node-gpg
