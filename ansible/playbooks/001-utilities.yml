-   name: Install common utilities
    hosts: localhost
    connection: local
    become: yes

    tasks:
    
        -   include_role:
                name: utilities
                task_from: control
